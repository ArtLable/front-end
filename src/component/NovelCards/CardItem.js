import React from 'react'


const CardItem = ({ card }) => {
    const {//가져올 json data image, b, c 
    } = card;

  return (
    <div className="CardItemBlock">
      { //image
      }
    </div>
  )
}

export default CardItem
